<table>
  <tr>
    <td><%= @quotes[rand(@quotes.length)].quote %></td>
  </tr>
</table>
<%= button_to 'Pearl of Wisdom', quotes_random_path, :method => :get %>
<%= button_to 'Click for a Funny', jokes_random_path, :method => :get %>